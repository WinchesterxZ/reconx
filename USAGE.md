# 📖 دليل الاستخدام الشامل لأداة ReconX (ReconX Usage Guide)

هذا الدليل يشرح بالتفصيل منطق عمل الأداة، كيفية التعامل مع **الوايلد كارد (Wildcard Domains)** مقابل **الأهداف المفردة (Single Target)**، شرح كل الأوبشنز والـ Flags، وما يصلح استخدامه مع كل سيناريو لتجنب الأخطاء الشائعة.

---

## 🎯 أولاً: منطق عمل الأداة مع أنواع الأهداف (Target Architecture)

### 1. كيف يتعامل ReconX مع الـ Wildcard Domain (`example.com` أو `*.example.com`)؟
> [!IMPORTANT]
> **منطق التسلسل الإجباري:** لا يمكن فحص مسارات أو ملفات جافاسكريبت مباشرة على Wildcard!
عند تمرير دومين رئيسي مثل `-d example.com` أو `-d *.example.com`:
1. **المرحلة الأولى (Subdomain Enum):** تقوم الأداة بتشغيل أدوات التجميع السلبي والإيجابي (`subfinder`, `amass`, `assetfinder`, `findomain`, `chaos`, `puredns`, `tlsx`, `massdns`) مع فحص الـ Wildcard DNS الذكي لعزل النتائج الوهمية.
2. **المرحلة الثانية (Alive Check):** يتم فحص كل الدومينات المكتشفة بواسطة `httpx` واستخراج فقط السيرفرات التي تعمل فعلياً وتستجيب (`Live Hosts`).
3. **المراحل اللاحقة (URLs, JS, DirFuzz, Params, CORS, Vuln):** يتم تشغيلها **فقط وحصرياً على السيرفرات الحية المؤكدة** (`store.Hosts`). لن تحاول الأداة إطلاقاً إرسال طلبات HTTP أو فحص JS على نص الوايلد كارد `*.example.com`.

---

### 2. كيف يتعامل ReconX مع الهدف المفرد (Single Target / Specific Host)؟
إذا كان لديك تارجت محدد ولا تريد إضاعة الوقت في البحث عن دومينات فرعية لشركة كاملة (مثلاً `admin.target.com` أو `https://target.com/`):
* **استخدم:** `--single` أو `-u https://target.com` أو `--skip-subs`.
* **ماذا يحدث في الكود؟**
  - يتم تخطي مرحلة الـ Subdomain Enumeration بالكامل فوراً.
  - يتم اعتبار الهدف فوراً كـ Live Target ويتم التحقق منه بـ `httpx`.
  - تبدأ مباشرة مراحل: سحب الروابط (`URLs`)، فحص الجافاسكريبت والـ Secrets (`JS`)، فحص المنافذ (`Ports`)، فحص المسارات (`DirFuzz` إن كان مفعلاً)، فحص الباراميترات (`Params`)، والبحث عن الثغرات (`Vulns`).

---

## 📋 ثانياً: جدول الأوبشنز الكامل وشرح استخداماتها

| الـ Flag | النوع | الوظيفة | التوافق والاستخدام المناسب |
| :--- | :--- | :--- | :--- |
| `-d domain.com` | إدخال | تحديد الدومين المستهدف (يمكن تكراره) | يُستخدم مع Wildcard أو Single Domain |
| `-u <url/host>` | إدخال | فحص هدف مفرد مباشرة (Shorthand) | **يتخطى Subdomain Enum تلقائياً** |
| `--single` | نمط | تفعيل نمط الهدف المفرد | يُستخدم مع `-d admin.target.com` لتخطي جمع الدومينات |
| `--ip <cidr>` | إدخال | فحص نطاق IP كامل (مثل `192.168.1.0/24`) | يتم تمريره لفحص المنافذ و httpx |
| `--asn <asn>` | إدخال | فحص أرقام الـ ASN (مثل `AS12345`) | يقوم بجلب نطاقات الـ IP التابعة له |
| `--scope scope.txt` | فلترة | تحديد نطاق الفحص المسموح والممنوع | يمنع فحص أي دومين خارج الـ Scope |
| `--skip-subs` | تخطي | تخطي مرحلة جمع الدومينات الفرعية | ممتاز عند امتلاك قائمة دومينات جاهزة |
| `--skip-alive` | تخطي | تخطي فحص السيرفرات الحية | لا يُنصح به إلا عند التأكد من حيوية الأهداف |
| `--skip-ports` | تخطي | تخطي فحص المنافذ (`naabu`) | لتسريع الفحص عند الرغبة في فحص الويب فقط |
| `--skip-urls` | تخطي | تخطي سحب الروابط والـ Endpoints | يستخدم للفحوصات السريعة |
| `--skip-js` | تخطي | تخطي تحليل ملفات الـ JS وسحب الـ Secrets | لتوفير الوقت في الفحص الأولي |
| `--skip-vuln` | تخطي | تخطي فحص الثغرات بـ `nuclei` | للاكتفاء بالاستطلاع بدون فحص ثغرات |
| `--screenshots` | اختياري (Opt-in) | التقاط صور لجميع السيرفرات الحية (`gowitness`) | تُحفظ في `screenshots/<domain>/` وتظهر بالتقرير |
| `--fuzz` / `--dirfuzz` | اختياري (Opt-in) | تفعيل فحص المجلدات (`feroxbuster`/`dirsearch`) | ⚠️ **معطل افتراضياً** لحماية البرامج الحساسة |
| `--params` | اختياري (Opt-in) | تفعيل كشف الباراميترات المخفية (`arjun`) | ⚠️ **معطل افتراضياً** لأنه نشط ومكثف |
| `--skip-cloud` | تخطي | تخطي كشف حاويات S3 و Cloud Buckets | افتراضياً يعمل تلقائياً |
| `--skip-cors` | تخطي | تخطي فحص ثغرات الـ CORS | افتراضياً يعمل تلقائياً |
| `--no-timeout` | تحكم | إلغاء الـ Timeouts للأدوات (تشغيل كامل) | **موصى به للأهداف الضخمة** (مثل Google, Uber) |
| `--resume <dir>` | استئناف | استكمال فحص سابق توقف أو تم قطعه | يتخطى المراحل التي اكتملت وكتبت ملفاتها |
| `--header <str>` | تخصيص | إضافة Header خاص بكل الطلبات | ممتاز للـ Bug Bounty (مثل `"X-Bug-Bounty: user"`) |

---

## 🚫 ثالثاً: إيه مينفعش مع إيه؟ (Incompatible & Bad Combinations)

> [!WARNING]
> تجنب هذه التركيبات غير المنطقية:

1. **`--single` مع `-d *.target.com` (خطأ):**
   * *السبب:* لا يمكن فحص هدف مفرد على صيغة الوايلد كارد بدون فكها لدومينات فرعية أولاً.
   * *الصحيح:* إذا كان التارجت وايلد كارد، شغل بدون `--single`.
2. **`--skip-alive` مع `--fuzz` أو `--params` بدون نتائج سابقة (خطأ):**
   * *السبب:* أدوات الفازينج تحتاج لسيرفرات حية (`Live Hosts`) لتختبر المسارات عليها.
3. **تشغيل `--fuzz` و `--params` على برامج Bug Bounty تمنع الـ Active Crawling (تحذير):**
   * *السبب:* بعض برامج المكافآت تعتبر الـ Directory Brute-force هجوماً مزعجاً قد يعرضك للحظر. لذا تم جعلهما **Opt-in** اختياريين تماماً.

---

## 💡 رابعاً: سيناريوهات التشغيل الشائعة (Real-world Scenarios)

### سيناريو 1: فحص شامل لبرنامج Bug Bounty كامل (Full Scope Recon)
لدينا دومين وايلد كارد `target.com`، ونريد تشغيل كل المراحل الأساسية بدون إزعاج السيرفرات بالفازينج:
```bash
./reconx -d target.com --header "X-Bug-Bounty: Winchester" --output ./scans
```

### سيناريو 2: فحص هدف مفرد محدد جداً (Single Host Deep Scan)
لدينا ساب دومين معين مثل `api.target.com` ونريد فحصه بعمق مع تخطي جمع الدومينات:
```bash
./reconx -u https://api.target.com --fuzz --params --output ./single-target
```
*أو:*
```bash
./reconx -d api.target.com --single --fuzz
```

### سيناريو 3: فحص فائق السرعة (Passive & Web Only)
تخطي فحص المنافذ وفحص الثغرات، والتركيز على جمع الدومينات والروابط وتحليل الـ JS:
```bash
./reconx -d target.com --skip-ports --skip-vuln
```

### سيناريو 4: فحص هجومي مكثف لأهداف ضخمة (No-Timeout Aggressive)
للشركات الكبرى حيث تحتاج الأدوات ساعات طويلة بدون أن يتم إنهاؤها بـ Timeout:
```bash
./reconx -d airbnb.com --no-timeout --fuzz --params -v
```

### سيناريو 5: استئناف فحص بعد انقطاع (Resume Mode)
إذا أغلقت الفحص أو توقف، استخدم مسار المجلد لاستكمال ما تبقى دون تكرار ما تم:
```bash
./reconx --resume ./scans/target.com-1774175844 --fuzz
```

---

## 🌐 خامساً: استخدام الواجهة الرسومية (Web Dashboard)
1. شغل سيرفر الويب من مسار `/web`:
   ```bash
   cd web && npm run dev
   ```
2. افتح المتصفح على `http://localhost:3000`.
3. ستجد خيارات تفعيل وإلغاء أي مرحلة بـ Switch واضح، وخيارات الفازينج والباراميترات معطلة افتراضياً للسلامة ويمكن تفعيلها بنقرة واحدة.
